== Inline templates
Is it possible to inline template attributes? We want attributes.

== Admin list
indent title column rename
Needs abstracting for model

== Admin forms
Term parents displaying the null option badly as '-1'
Term element, Term parents - no verification term exists.

Humm. Verification (for what?) not hard to add.

Validate tree_id never chages for a term.

== API
get() query
deletes to be enabled
Reparent_choices is a mess

== Rendering
Why are dispalys sometimes hanging?

== SVGs
Can make relative sizing?

== Models
Abstract models
Get rid of slugs, they're for abstraction
Reverse lookupsb
